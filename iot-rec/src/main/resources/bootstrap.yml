spring:
  application:
    name: ioAppReceive
  profiles: dev, devcom, test, testswarm
  cloud:
    config:
      #uri:


      uri: ${CONFIG_URI}
      env: default
      label: master
      password: mirko
      username: mirko
